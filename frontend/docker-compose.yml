services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2000:2000"
    environment:
      - VITE_PORT=2000
      - VITE_BACKEND_URL=http://localhost:8000
    networks:
      - newnetwork

networks:
  newnetwork:
    external: true