services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2000:2000"
    environment:
      - VITE_PORT=${VITE_PORT}
      - VITE_BACKEND_URL=${VITE_BACKEND_URL}
    networks:
      - newnetwork

networks:
  newnetwork:
    external: true